// The (frontend) route group is where your friend will build all the UI.
// This is just a placeholder layout — your friend should replace this.

export default function FrontendLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
